// notifications.js (legacy) 廃止:
// 永続通知 API / WebSocket 'notification' メッセージは削除済み。
// 互換性のため空スタブを残しているが、今後削除予定。
// 参照している既存コードが万一あっても副作用を発生させない。
console.debug('notifications.js legacy stub loaded (no-op)');
