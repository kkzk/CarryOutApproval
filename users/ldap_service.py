"""(Deprecated) ldap_service module.

All LDAP approver lookups have been migrated to authentication-time provisioning
and DB queries in users.utils.get_approvers_for_user.
Remaining imports should be removed; this stub stays temporarily to avoid import errors.
"""
